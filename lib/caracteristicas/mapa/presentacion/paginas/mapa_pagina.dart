import 'package:flutter/material.dart';

class MapaPagina extends StatelessWidget {
  const MapaPagina({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Página del Mapa'),
      ),
    );
  }
}
