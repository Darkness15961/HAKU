import 'package:flutter/material.dart';

class RutasPagina extends StatelessWidget {
  const RutasPagina({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Página de Rutas'),
      ),
    );
  }
}
